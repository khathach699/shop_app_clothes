enum TextSize { small, medium, large }

enum OrderStatus { pending, processing, shipped, delivered, cancelled }

enum PaymentMethod {
  cashOnDelivery,
  creditCard,
  paypal,
  googlePay,
  visa,
  paytm,
  applePay,
  masterCard,
  paystack,
}
